<UserControl x:Class="FeatureHub.Feature2Control"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          Background="#fdfcfa">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Images/BigClock.jpg" Opacity="0.2"/>
        </Grid.Background>
        <StackPanel HorizontalAlignment="Center">
            <TextBlock x:Name="TimeDisplay" FontSize="68" FontWeight="Bold" Margin="10" TextAlignment="Center" Foreground="#242931"/>
            <ComboBox x:Name="TimeZoneComboBox" Margin="10" SelectionChanged="TimeZoneComboBox_SelectionChanged" Background="#1d6d80" Foreground="#242931" FontSize="48" SelectedIndex="0">
                <ComboBoxItem Content="Brussels"/>
                <ComboBoxItem Content="UTC"/>
                <ComboBoxItem Content="CEST"/>
            </ComboBox>
        </StackPanel>
    </Grid>
</UserControl>